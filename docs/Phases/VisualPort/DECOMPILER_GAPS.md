# Decompiled C# and Native Recovery Gaps

The cleaned C# update removed obvious decompiler issue markers but still contains IL annotations, invalid type flow, default/null placeholders, and empty files. This register prevents damaged bodies from becoming accidental runtime specifications.

| ID | Class | Method | Question | Known evidence | Missing evidence | Impact | Next investigation |
|---|---|---|---|---|---|---|---|
| DG-001 | `kairo.unity.ui.Graphics` | all, especially `DrawImage`, `SetClip`, `SetColor`, `Scale` | What are the exact clip, blend, color, scale, and source/destination pixel semantics? | Update `Graphics.cs` is zero bytes; native dump has declarations/RVAs. | Native trace or golden raster fixtures for each overload. | Blocks any trustworthy browser drawing backend. | Recover native calls on the pinned APK and compare small raster fixtures. |
| DG-002 | `kairo.unity.ui.Sprite` | property getters and `get_Item`/`set_Item` | Are every field index and signed/packed conversion exactly as the source format expects? | Field names/index intent are visible; native getter RVAs exist; `get_Item` body is damaged. | Native field reads plus OPT/SEB fixture correlation. | A wrong crop/offset/flip corrupts every visual class. | Trace all getters against decoded SEB records before implementation. |
| DG-003 | `kairo.unity.ui.Seb` | `GetSprites`, `GetSprite`, `Render*`, `Draw*` | How are SEB layers, frames, anchors, and draw variants composed? | Method declarations, native RVAs, and SEB format guide exist. | Native traces and expected composite images for multi-layer/multi-frame SEBs. | Blocks exact character/furniture parity. | Build a fixture matrix from `seb_codec.py` and compare native output. |
| DG-004 | `kairo.unity.ui.Image` | `LoadOptimize`, `GetOptimize`, `GetOptimizeSeb`, `Use`, `Unuse`, `Resize` | Which OPT records own pixels and how are SEB links/expiration/atlas states updated? | OPT guide/codecs and native RVAs exist; C# has invalid flow. | Native state traces and round-trip fixtures. | Blocks source-faithful image/resource ownership. | Trace one raw OPT, one reconstructed image, and one linked SEB end to end. |
| DG-005 | `kairo.unity.ui.ImageAtlas` / `ImageAtlasManager` | packing, compaction, `Load`, atlas-to-image methods | Does atlas packing affect only storage or also source crop and draw coordinates? | Native method surface and atlas declarations exist. | Atlas fixture before/after compaction and native pixel comparison. | Could introduce silent crop/offset drift. | Verify atlas round-trip with untouched Sprite fields. |
| DG-006 | `kairo.unity.ui.ResourceManager` | `Load`, `LoadReady`, `LoadStart`, `DrawSeb*`, `RenderSeb*` | Which group/file state transitions are required before a draw is valid? | `AppData` group fields, `img`/`seb` arrays, and native wrappers are known. | Native load-state trace for every visual group. | A replacement may render the right ID from the wrong group. | Capture `resChip_`, `resHuman_`, and `resGame_` load/draw sequences. |
| DG-007 | `game.Room` | `Draw`, `Update`, construction helpers | What is the complete general-room pass order and which branches are gameplay-only? | Bounded native room contracts and draw RVAs exist; decompiled body has extensive IL damage. | Native call trace across more than the pinned closure scene. | General room parity and safe gameplay cut remain unresolved. | Trace one small and one multi-cell room with native method hooks. |
| DG-008 | `game.ObjChip` | `DrawWall`, `Draw` overloads, `PlaceObj` | How do type, direction, frame, parent, wall, and door branches select SEB/image records? | Native wall/object RVAs, strict closure, and source resource accesses exist. | Per-furniture fixture matrix including every direction and parent shape. | Furniture/object port could overfit current room IDs. | Correlate `FurnitureData` selectors to native Draw calls. |
| DG-009 | `game.Staff` | `Draw`, `DrawStaff`, `ChangeSeb`, `AdvanceSebFrame`, `Update` | What are exact action/direction/scale/alpha/frame timing rules? | Native draw RVAs, `resHuman_` accesses, and character asset metadata exist. | Native animation trace and golden frames for each visible action. | Full character layer cannot be declared parity-ready. | Start with one staff identity and idle/walk/action transitions. |
| DG-010 | `main.AppData` / `form.GameForm` / `main.Main` | draw helpers and lifecycle methods | Which shared helper and lifecycle calls are required for a scene draw? | Native draw/lifecycle RVAs and source call sites exist. | Native call order for a complete frame and screen-state changes. | Backend boundary may be placed too high or too low. | Trace `OnCreate → OnUpdate → OnDraw → Room.Draw` on the pinned build. |
| DG-011 | `game.Camera` | position/base/target methods | Are camera targets and base offsets mutated by gameplay or only by visible scene setup? | Source declarations and native getters/setters exist. | Native trace with room transitions and UI overlays. | Incorrect camera ownership shifts every draw. | Compare camera values at the first native room frame. |
| DG-012 | room/resource asset coverage | helper/avatar/event groups | Which source groups are required for a complete visual scene beyond the bounded closure? | Asset fingerprint has 554 SEB, 411 OPT, and named resource groups; current runtime intentionally approves a small subset. | Group-to-asset manifest and native load evidence for unclassified assets. | Do not promote unresolved assets or infer their semantics. | Expand only from native load traces and source selectors. |

## Gate

No gap is closed by a plausible decompiler reconstruction. A gap closes only when native behavior, source-format data, and a repeatable fixture agree.
