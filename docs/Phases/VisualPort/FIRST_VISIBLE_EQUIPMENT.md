# First-visible starter equipment

Status: bootstrap presence `SOURCE_BACKED`; first-visible persistence `SOURCE_LIMITED`.

After inserting the NewGame room, `AppData.NewGame` scans FurnitureData entries with the initial-place flag and fills the first empty raw type-1 slots. The pinned room-0 result is:

- FurnitureData `12`, Trash Can, cell `[8,5]`, SEB `21`, image `109`.
- FurnitureData `26`, Old Printer, cell `[8,6]`, SEB `21`, image `106`.
- FurnitureData `56`, Calendar, cell `[2,7]`, SEB `21`, image `127`.

This static evidence disproves absence from the NewGame bootstrap. It does not prove that tutorial or event code leaves those objects in place for the first stable main display. No later hide, move, removal, or replacement is inferred from screenshots or from the damaged EventData interpreter.

Evidence: [starter-equipment-contract.json](../../../knowledge/fixtures/accepted/visual-port/first-visible-starter/starter-equipment-contract.json).
