# I0 Implementation Plan

Status: implemented in the inline runtime workspace.

The I0 implementation consumes the immutable R0 contract package and the generated canonical catalog. The delivery order is foundation and data ownership, movement and arrival dispatch, desk startup, tick/RNG autonomy, equipment and talk loops, HP/home recovery, planning boundaries, cleanup, and scenario acceptance.

The production living owner is `runtime/social-dev/src/core/living`. The existing renderer-facing `ActorState` remains a derived projection compatibility surface. V8, MapChip, Renderer, save compatibility, browser/server execution, emulator proof, and product/dashboard policy remain outside this phase.

Acceptance is based on the pinned source identity, the R0 hash lock, exact catalog counts (141/30/36/103), focused Vitest coverage, the independent Python acceptance gate, and runtime-generated S1–S10 transition traces.
