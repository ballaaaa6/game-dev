# Runtime

โค้ดจำลองที่อ่าน knowledge contracts แล้วทำงานต่อเนื่อง โดยไม่ execute decompiled C# โดยตรง

- `office/` — ฉาก office, actor และ deterministic simulation baseline
- `dashboard/` — task queue, assignment และ dashboard interaction baseline
