# Phase 3 — Language extraction และ translation layer

สถานะ: รอเริ่ม; CSV ปัจจุบันถูกตรวจ BOM/UTF-8 แล้วใน Phase 0

พื้นที่เตรียมไว้:

- `docs/kairosoft_language_system.md` — reference จาก CSV และ dump ปัจจุบัน
- `references/` — วาง locale schema, mapping และ QA reference ที่สร้างใน phase นี้
- `artifacts/` — วาง `language.json`, locale files และ QA report

source language tables ยังคงอยู่ใน `game-dev-story-mod_Sprites/language/` และ baseline อยู่ที่ `Phase0/artifacts/language_manifest.json`
