# Phase 4 — Code study และ logic classification

สถานะ: รอเริ่มหลัง asset และ animation catalog

วาง `legacy_to_agent_mapping.md`, function classification และ state traces ใน `docs/` หรือ `artifacts/` ตามชนิดไฟล์ โดยอ้าง evidence จาก `game-dev-story-mod_Dumped/`
