# Phase 5 — Minimal web office runtime

สถานะ: ยังไม่เริ่ม

วาง room/agent runtime manifests, web-only adapters และ visual test report ใน `artifacts/` และ `docs/`; source asset เดิมไม่ย้ายเข้ามา ให้ reference path กลับไปยัง source root
