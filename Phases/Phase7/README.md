# Phase 7 — เชื่อม AI model

สถานะ: ทำท้ายสุดหลัง visual และ task system เสถียร

วาง model adapter, persona/prompt, tool permissions, memory schema และ decision logs ใน `artifacts/` และ `docs/`; ไม่ให้ model ควบคุมการวาด sprite โดยตรง
